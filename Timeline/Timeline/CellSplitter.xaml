<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Timeline.CellSplitter">
    <ViewCell.View>
        <StackLayout 
            HorizontalOptions="Center"
            VerticalOptions="Fill"
            Orientation="Horizontal"
            Margin="10,10,10,0"
            Spacing="0">
            <!-- Formatted text, split in segments -->
            <Label TextColor="#7E7E7E"  Text="{Binding Duration.Minutes}"/>
            <Label TextColor="#7E7E7E"  Text=" Min "/>
            <Label TextColor="#7E7E7E"  Text="{Binding Duration.Seconds}"/>
            <Label TextColor="#7E7E7E"  Text=" Sec"/>
        </StackLayout>
    </ViewCell.View>
</ViewCell>